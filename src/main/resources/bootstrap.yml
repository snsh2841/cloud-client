spring:
  profiles:
    active: development
  cloud:
    config:
      uri: http://localhost:8888
      username: user
      password: password
  application:
    name: config-client
server:
  port: 8081